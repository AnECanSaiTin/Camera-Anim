package cn.anecansaitin.cameraanim.client.enums;

public enum ControlType {
    /** Left control point of a Bezier curve. */
    LEFT,
    /** Right control point of a Bezier curve. */
    RIGHT,
    /** No control point (direct keyframe position). */
    NONE
}